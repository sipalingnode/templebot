{
  "choiceArguments": {
    "expectedAdmin": "{{dso}}",
    "transfer": {
      "sender": "{{investor}}",
      "receiver": "{{investor}}",
      "amount": "{{amount}}",
      "instrumentId": {
        "admin": "{{dso}}",
        "id": "{{instrument_id}}"
      },
      "requestedAt": "{{time}}",
      "executeBefore": "{{timeEnd}}",
      "inputHoldingCids": "{{holding_ids}}",
      "meta": {
        "values": {}
      }
    },
    "extraArgs": {
      "context": {
        "values": {}
      },
      "meta": {
        "values": {}
      }
    }
  },
  "excludeDebugFields": true
}